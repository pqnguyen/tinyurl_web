import pagePaths from "src/constants/pagePaths";
import App from "src/App";

const routeMapping = [
    {
        "path": pagePaths.HOME,
        "component": App
    }
];

export default routeMapping;
